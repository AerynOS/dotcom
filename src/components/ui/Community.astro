---
import CommunityCard from "./CommunityCard.astro";
import { COMMUNITY } from "@/data/constants";
---

{
  COMMUNITY.map((community) => (
    <CommunityCard
      heading={community.title}
      content={community.description}
      icon={community.icon}
      linkURL={community.url}
    />
  ))
}
